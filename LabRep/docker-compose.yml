version: '3.8'
services:
  # labreport-backend:
  #   build:
  #     context: .
  #     target: development
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - SERVER_PORT=8080
  #     - MSSQL_URL=jdbc:sqlserver://sql-server-db:1433;databaseName=DBlabReport;integratedSecurity=false;encrypt=true;trustServerCertificate=true;
  #   volumes:
  #     - ./:/app
  #   depends_on:
  #     - sql-server-db
  sql-server-db:
    container_name: mssqlserver
    image: mcr.microsoft.com/mssql/server:2019-latest
    ports:
      - "1453:1433"
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=Strongpassword123
      - MSSQL_PID=Express
      - MSSQL_USER=mayliba
      - MSSQL_PASSWORD=Strongpassword123
      - MSSQL_DATABASE=DBlabReport
#     volumes:
#       - mssql_data_2022:/var/opt/mssql/data
#       - mssql_log_2022:/var/opt/mssql/log
# volumes:
#   mssql_data_2022:
# docker compose -f docker-compose.yml up --build

# docker-compose logs labreport-backend
